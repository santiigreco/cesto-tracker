import React from 'react';
import { HomeVisualShowcase } from './HomeVisualShowcase';

/**
 * @deprecated Use HomeVisualShowcase instead. Maintained for backwards compatibility.
 */
export const HomePhonePreview: React.FC = () => {
  return <HomeVisualShowcase />;
};
